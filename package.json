{
  "name": "leetcode-daily-coach",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "LeetCode Daily Coach (Ascent) — 每日推播的演算法課程引擎",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate:curriculum": "tsx scripts/validate-curriculum.ts",
    "validate:problem-bank": "tsx scripts/validate-problem-bank.ts",
    "generate:schedule": "tsx scripts/generate-schedule.ts",
    "validate:schedule": "tsx scripts/validate-schedule.ts",
    "validate:content": "tsx scripts/validate.ts",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
